{
  "name": "trading-bot",
  "module": "src/main.ts",
  "type": "module",
  "scripts": {
    "dev": "bun run src/main.ts",
    "dev:watch": "bun run --watch src/main.ts",
    "format": "prettier \"src/**/*.ts\" --write"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "prettier": "^3.5.3"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}
